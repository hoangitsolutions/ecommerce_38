<%= render "shared/error_messages", object: f.object %>
<%= f.label t ".name" %>
<%= f.text_field :name %>

<%= f.label t ".parent_id" %>
<%= f.select :parent_id, options_for_select(display_categories, selected: @category.parent_id) %>

<%= f.submit t(".submit"), class: "btn btn-primary" %>